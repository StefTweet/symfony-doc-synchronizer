{% extends '::base.html.twig' %}

{% block body %}
    <h1>Hello world!</h1>

    <div class="row">
        <div class="col-md-6">
            <table>
                <thead>
                    <tr>
                        <td>filename</td>
                    </tr>
                </thead>
                <tbody>
                    {% for file in files %}
                        <tr>
                            <td>{{ file.name }}</td>
                        </tr>
                    {% endfor %}
                </tbody>
            </table>
        </div>
        <div class="col-md-6">
            <table>
                <thead>
                    <tr>
                        <td>filename</td>
                    </tr>
                </thead>
                <tbody>
                    {% for file in files %}
                        <tr>
                            <td>{{ file.name }}</td>
                        </tr>
                    {% endfor %}
                </tbody>
            </table>
        </div>
    </div>
{% endblock %}
